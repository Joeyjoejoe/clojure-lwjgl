FLOAT_SIZE_BYTES = 4;

VECTOR4F_SIZE_BYTES = 4 * FLOAT_SIZE_BYTES;

MATRIX_SIZE_FLOATS = 4 * 4;

MATRIX_SIZE_BYTES = MATRIX_SIZE_FLOATS * FLOAT_SIZE_BYTES;

INSTANCE_SIZE_BYTES = MATRIX_SIZE_BYTES * 2 + FLOAT_SIZE_BYTES * 2 + FLOAT_SIZE_BYTES;

INSTANCE_SIZE_FLOATS = MATRIX_SIZE_FLOATS * 2 + 3;

